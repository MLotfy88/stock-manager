# دلائل الأوامر البرمجية وتصميم الواجهات للمشروع (Project Prompts)

هذا الملف يحتوي على الأوامر (Prompts) الشاملة والكاملة لتطبيق إدارة المخزون (Stocky Management). يمكنك نسخ هذه الأوامر للحصول على بداية سريعة سواء عند طلب بناء المشروع برمجياً من نماذج الذكاء الاصطناعي، أو عند تكليف مصمم واجهات (UI/UX) للعمل على المشروع.

---

## 1. موجه البناء البرمجي (Master Development Prompt)
استخدم هذا الموجه (Prompt) إذا أردت بناء أو إعادة كتابة المشروع من الصفر بواسطة الذكاء الاصطناعي.

**[انسخ النص التالي:]**

```text
أريد بناء تطبيق متكامل لإدارة المخزون والمستلزمات (Stock Management System) يدعم العمل بدون إنترنت كخيار أول (Offline-First) مع مزامنة سحابية سلسة.

**المواصفات التقنية (Tech Stack):**
- **الواجهة الأمامية (Frontend):** Vite + React 18 + TypeScript.
- **تنسيق الواجهة (Styling & UI):** Tailwind CSS + مكتبة shadcn/ui و Radix UI + Lucide React للأيقونات + Framer Motion للأنيميشن.
- **إدارة الحالة وجلب البيانات (State & Data Fetching):** TanStack React Query للبيانات السحابية، و React Hook Form + Zod لإدارة النماذج والتحقق من صحتها المستمرة.
- **قواعد البيانات (Database):** 
  - محلياً (Local): Dexie.js (IndexedDB) لدعم العمل بدون إنترنت.
  - سحابياً (Cloud): Supabase للـ Backend-as-a-service (قاعدة البيانات الأساسية، المصادقة، والـ Storage).
- **التوافقية وتطبيقات الموبايل (Cross-Platform):** Capacitor لبناء تطبيق Android native مع استخدام Capacitor Camera Plugins لدعم قراءة الباركود بشكل مباشر.

**الوحدات الأساسية المطلوبة (Core Modules):**
1. **المخازن والمستلزمات (Stores & Supplies):** 
   - إضافة وإدارة المخازن المتعددة.
   - إدارة الأصناف، أنواع المستلزمات (Supply Types)، الشركات المصنعة، وحزم التعبئة (Packages).
   - النقل بين المخازن المتعددة (Transfer Inventory).
2. **الاستهلاك والجرد (Consumption & Reorder Points):** 
   - تسجيل الاستهلاك اليومي.
   - نظام جرد الرفوف وتقارير المخزون المتقدمة.
   - الذكاء الآلي لتحديد نقاط إعادة الطلب (Reorder Points) والتنبيه عند انخفاض المخزون.
3. **الديون والموردين (Suppliers & Debt Management):** 
   - إدارة حسابات الموردين وتقييم الأداء (Supplier Performance).
   - إدارة الديون مع "تقويم للديون" (Debt Calendar) لمتابعة مواعيد الاستحقاق وجدولة الدفعات.
4. **الفوترة والعمليات الخاصة (Invoicing & Vouchers):** 
   - إنشاء الفواتير المباشرة من الرف (On-Shelf Invoicing).
   - إدارة المرتجعات (Returns Management).
   - قسائم استبدال التوالف (Replacement Vouchers).
5. **لوحة التحكم وصلاحيات الوصول (Admin & Dashboards):** 
   - شاشة Admin لإدارة المستخدمين والصلاحيات المختلفة.
   - لوحات تحكم متخصصة وملخصات (User Dashboard/Management).
6. **الاستيراد والتصدير:**
   - دعم التصدير والاستيراد لملفات Excel/CSV عبر (xlsx و papaparse).

**مميزات رئيسية يجب توفرها:**
- قارئ باركود مدمج يدعم الكاميرا أو أجهزة السكانر الخارجية لمعالجة إدخال الأصناف السريع.
- يجب تطبيق معمارية Offline-First بشكل كامل، أي يتم حفظ كل التحديثات على Dexie، وعند الاتصال بالإنترنت تتم المزامنة مع Supabase في الخلفية.

الرجاء التخطيط الدقيق للمشروع، والبدء بهيكلة مجلدات المشروع (Folders Structure) ثم بناء قاعدة البيانات وتصميم الـ Schema، والانتقال خطوة بخطوة في إنشاء الـ Components والمكونات بأسلوب Clean Code و Modular Design.
```

---

## 2. موجه تصميم واجهة المستخدم (UI/UX Designer Prompt)
استخدم هذا الموجه (Prompt) عند التواصل مع مصمم واجهات المستخدِم لضمان حصولك على تجربة مستخدم وتصميم شامل لجميع احتياجات المشروع.

**[انسخ النص التالي:]**

```text
أنا بحاجة إلى تصميم شامل لنموذج واجهة مستخدم وتجربة مستخدم (UI/UX) لتطبيق متخصص في إدارة المخزون والمستلزمات التجارية/الطبية. 
يجب أن يكون التصميم متجاوباً ليعمل كتطبيق ويب (Responsive Web App) وتطبيق موبايل هجين (Hybrid Mobile App/Android).

**النطاق العام للتصميم (Design Scope):**
- تصميم لوحة تحكم (Dashboard) احترافية وعملية جداً (Heavy Data Entry).
- التصميم يجب أن يكون نظيفاً (Clean, Minimalist) ومبنياً على أنظمة تصميم حديثة (مثل تصميمات shadcn/ui أو Vercel)، باستخدام ألوان هادئة تريح العين عند الاستخدام المطول مع توفير الوضع الليلي (Dark Mode).

**الشاشات الرئيسية المطلوبة (Key Screens):**
1. **شاشة الدخول والمصادقة (Login & Auth).**
2. **لوحة القيادة (Dashboard):** تعرض إحصائيات المخزون، تنبيهات حول نقص المواد، إجمالي الديون المستحقة، واستعراض سريع لحركة المخزونات.
3. **إدارة المخزون والمستلزمات (Inventory Hub):**
   - جدول (Data Table) احترافي لاستعراض الأصناف، كمياتها، أماكن تخزينها، وحالتها (يدعم الفلاتر، الترتيب، البحث، والترقيم).
   - شاشة إضافة/تعديل صنف (تشمل البيانات الأساسية، الباركود، حزم التعبئة، وتحديد المورد/المُصنع).
4. **تتبع الاستهلاك (Consumption):**
   - واجهة سريعة لإدخال الكميات المستهلكة مسحوبة من المخزن بأقل عدد من النقرات (تدعم السكان باستخدام الباركود).
5. **نظام الفوترة والجرد (Invoicing & Auditing):**
   - شاشة العمليات وإصدار الفواتير مباشرة من الرف (On-Shelf Invoicing).
   - تقارير جرد البضاعة.
6. **الديون والموردين (Debt & Suppliers):**
   - ملف المورد (Supplier Profile) يشمل معلوماته وتقييم أدائه بمرور الوقت.
   - واجهة "تقويم الديون" (Debt Calendar) تعرض تواريخ الدفعات والاستحقاقات على شكل نتيجة (Calendar View).
7. **العمليات العكسية:**
   - شاشات إدارة المرتجعات للموردين (Returns).
   - قسائم استبدال البضاعة التالفة (Replacement Vouchers).
8. **شاشات إعدادات الإدارة (Admin Settings):**
   - إدارة المخازن (إنشاء وربط المخازن ببعضها).
   - إدارة المستخدمين والصلاحيات.

**متطلبات خاصة لتجربة المستخدم (UX Requirements):**
- **إدخال البيانات السريع:** التركيز في التصميم على سرعة إدخال البيانات باستخدام الكيبورد بشكل كبير وقارئ الباركود، وتقليل عدد النقرات.
- **حالات عدم الاتصال (Offline States):** واجهة التطبيق يجب أن توضح للمستخدم بوضوح متى يكون غير متصل بالإنترنت، مع طمأنته بأن بياناته يتم حفظها محلياً وبانتظار المزامنة (Cloud Sync Indicators).
- **التوافقية:** توضيح كيف سيكون شكل الجداول والعمليات المعقدة والمنبثقات (Modals) عند تصفح التطبيق من الموبايل.

يرجى تسليم التصميم على هيئة مشروع مجمّع (Figma Prototype)، يحتوي على مسارات المستخدمين الأساسية (User Flows)، وملف منفصل أو قسم يحتوي على نظام التصميم ومجموعة المكونات الأساسية (Design System Toolkit: أزرار، حقول إدخال، فلاتر، الوان، وغيرها).
```
